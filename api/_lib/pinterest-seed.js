/** Catalogo pin per coda API (generato da buffer-queue-tool/pins-data.json). */

import { createRequire } from 'module';

const require = createRequire(import.meta.url);

export const PINTEREST_SEED_CATALOG = require('./pinterest-catalog.json');
